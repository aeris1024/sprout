---
id: TASK-36
title: 全スナップショットをSprout非依存の静的アーカイブへ一括展開する
status: To Do
assignee: []
created_date: '2026-07-29 19:11'
updated_date: '2026-07-29 19:46'
labels: []
dependencies:
  - TASK-21
  - TASK-22
  - TASK-38
references:
  - src/sprout/repository.py
  - src/sprout/cli.py
  - README.md
priority: medium
type: feature
ordinal: 37000
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
## 背景

将来Sproutを使用しなくなった場合でも、.sproutに保存された過去ファイルと履歴情報へアクセスできる退避手段が必要である。現在は単一コミットをexportできるが、分岐や削除済みブランチを含む全スナップショットを、SproutやPythonを必要としない通常のファイル群としてまとめて展開する機能はない。

## ゴール

DBに残る全コミットをコミット日時とコミットIDで識別できるフォルダへ展開し、静的HTMLからコミットの親子関係、メタデータ、メモ、ラベル、ファイル一覧、展開済みファイル、サムネイルへアクセスできるSprout非依存のアーカイブを生成する。

出力物は別環境へコピーした後も、ローカルのWebブラウザとファイルマネージャーだけで閲覧できることを重視する。サーバー、CDN、Sprout固有ランタイムへの依存を持たせない。

## 対象範囲と注意

- 現在のブランチから到達できる履歴だけでなく、削除済みブランチ由来を含むDB上の全コミットを対象とする。
- 日時だけでは同時刻や分岐で衝突するため、各スナップショットの出力名には短縮コミットIDも含める。
- コミット、ファイル、添付、メモ、ラベルの情報はHTMLに加え、機械可読なJSONにも保存する。
- 通常ファイルへの完全展開では圧縮と重複排除の効果が失われるため、実行前に推定展開容量と出力先の空き容量を確認できるようにする。
- 空ディレクトリ、権限、所有者、シンボリックリンクなど、Sproutが保存していない情報は再現対象外とする。
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [ ] #1 DBに存在する全コミットが、削除済みブランチ由来の到達不能コミットを含めて漏れなく展開される
- [ ] #2 各スナップショットはUTCのコミット日時と短縮コミットIDを含む衝突しないディレクトリ名を持ち、コミット時の相対パス構造とファイル内容が再現される
- [ ] #3 生成された静的HTMLで全コミットの親子関係、日時、メッセージ、ID、ブランチ、タグ、メモ、ラベルを確認でき、各スナップショットと展開済みファイルへ相対リンクでアクセスできる
- [ ] #4 保存された画像・音声等の添付が元ファイル名とメディア種別を維持して書き出され、対応するコミットから閲覧または再生できる
- [ ] #5 コミット、ファイル、添付、メモ、ラベルのメタデータが、親ID、パス、サイズ、SHA-256を含むSprout非依存のJSONとして保存される
- [ ] #6 HTMLと付随アセットは外部サーバー、CDN、Sprout、Pythonを必要とせず、別の場所へコピーした後もローカルで閲覧できる
- [ ] #7 実行前にコミット数、作成予定ファイル数、推定展開容量、出力先の空き容量を確認でき、容量不足を明確に報告する
- [ ] #8 既存の空でない出力先を意図せず上書きせず、中断された出力を識別でき、安全に再実行できる
- [ ] #9 展開中にオブジェクトの欠落または破損を検出した場合は対象を特定して失敗し、正常完了したように見えるアーカイブを残さない
- [ ] #10 コマンドの使用方法、出力構成、容量増加、Sproutが保存していないメタデータの制限がREADMEに記載され、全履歴、分岐、HTML、JSON、添付、メモ、ラベル、失敗時の安全性が自動テストで検証される
- [ ] #11 アーカイブにrepository_idとrepository_created_atが保存され、別の場所へコピーした後も元リポジトリを一意に識別できる
<!-- AC:END -->
